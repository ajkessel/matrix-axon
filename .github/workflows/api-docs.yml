name: No-Op Placeholder

on:
  push:
    branches: [ "ignore-all-branches-permanently" ] # Prevents automatic triggers

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Do nothing
        run: echo "This is a placeholder workflow pennding approval of PR 189."
